# docs/

Project documentation. Not the published site.

Published site generates to `output/site/`, via `generate_site.py`.

GitHub Pages deploys from a GitHub Actions workflow (`.github/workflows/pages.yml`), not from
this folder. `docs/` keeps its meaning as the project's documentation directory.

See `docs/file-index.md` for per-file detail.
